#Program #1:
#Clara Riley
#Luke Snell
#11/13/24

import tkinter as tk

window = tk.Tk()
window.title("Favorite Saying")

saying_label = tk.Label(window, text="If at first you don't succeed, don't try skydiving.")
saying_label.pack(padx=20, pady=20)

window.mainloop()
